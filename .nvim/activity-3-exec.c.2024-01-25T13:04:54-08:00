#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <wait.h>
int main() {
  pid_t pid = fork();
  if (pid == 0) {
    // Child

  } else if (pid > 0) {
    // Parent
    
  } else {
    
  }
  return 0;
}
